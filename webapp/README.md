# ROFL Bot - Мини-приложение

Красивое мини-приложение для Telegram бота с liquid glass эффектом.

## Настройка

1. **Установите зависимости:**
   ```bash
   pip install aiohttp
   ```

2. **Настройте URL мини-приложения:**
   
   Для локальной разработки используйте ngrok:
   ```bash
   ngrok http 8080
   ```
   
   Затем установите переменную окружения:
   ```bash
   export WEBAPP_URL=https://your-ngrok-url.ngrok.io
   ```
   
   Или укажите напрямую в `bot.py` в строке с `webapp_url`.

3. **Запустите бота:**
   ```bash
   python bot.py
   ```

4. **Откройте мини-приложение:**
   - Отправьте команду `/start` боту
   - Нажмите кнопку "📊 Дашборд"
   - Мини-приложение откроется в Telegram

## Функционал

- 📊 Статистика сообщений (всего, изменено, удалено)
- 🔍 Фильтры по периоду и типу события
- 📥 Экспорт данных в CSV, JSON, TXT
- 🎨 Красивый liquid glass дизайн
- 📱 Адаптивный интерфейс

## Структура файлов

- `index.html` - основная страница
- `style.css` - стили с liquid glass эффектом
- `app.js` - логика приложения

## API

### POST /api/messages
Получение истории сообщений для пользователя.

**Request:**
```json
{
  "user_id": 123456789
}
```

**Response:**
```json
{
  "messages": [
    {
      "type": "edited",
      "author": "Имя пользователя",
      "content": "Новый текст",
      "old_content": "Старый текст",
      "timestamp": 1234567890
    }
  ]
}
```
