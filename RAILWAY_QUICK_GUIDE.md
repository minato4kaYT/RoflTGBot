# Быстрая инструкция: Куда нажимать на Railway

## 🚀 Развертывание проекта

### Если проект еще не создан:

1. **Выйдите из Settings:**
   - Нажмите на название проекта "ample-reflection" вверху (или на "Architecture" в меню)

2. **Создайте новый сервис:**
   - Нажмите кнопку **"+ New"** (справа вверху)
   - Выберите **"GitHub Repo"** или **"Empty Project"**
   - Если GitHub Repo - подключите ваш репозиторий
   - Railway автоматически определит Python проект и запустит его

### Если сервис уже создан:

1. **Откройте ваш сервис:**
   - В разделе "Architecture" нажмите на название вашего сервиса (например, "rofl-bot")

2. **Настройте домен:**
   - Откройте вкладку **"Settings"** (в меню сервиса)
   - Найдите раздел **"Networking"**
   - Нажмите **"Generate Domain"** или **"Custom Domain"**
   - Скопируйте созданный URL (например: `https://rofl-bot-production.up.railway.app`)

3. **Обновите WEBAPP_URL:**
   - Вернитесь в **Project Settings** → **Shared Variables**
   - Или в настройках сервиса → **Variables**
   - Добавьте переменную: `WEBAPP_URL` = `https://your-app.up.railway.app/`
   - Или обновите в `config.py` и сделайте новый коммит

## 📍 Где что находится:

### Главное меню проекта (слева):
- **Architecture** - здесь видны все сервисы
- **Observability** - метрики и мониторинг
- **Logs** - логи работы
- **Settings** - настройки проекта

### В настройках сервиса:
- **Settings** → **Networking** → Generate Domain (для публичного URL)
- **Settings** → **Variables** → добавить переменные окружения
- **Deployments** → посмотреть историю деплоев
- **Logs** → посмотреть логи работы

## ✅ Проверка работы:

1. **Откройте Logs:**
   - В меню сервиса нажмите **"Logs"**
   - Должна быть строка: `HTTP server started on http://0.0.0.0:XXXX`
   - Должна быть строка: `Bot starting polling...`

2. **Проверьте веб-приложение:**
   - Откройте URL, который создал Railway
   - Должна открыться главная страница мини-приложения

3. **Проверьте бота:**
   - Откройте Telegram
   - Найдите вашего бота
   - Отправьте `/start`
   - Бот должен ответить

## 🔄 Если нужно обновить код:

1. Сделайте изменения в коде
2. Сделайте коммит и пуш в GitHub:
   ```bash
   git add .
   git commit -m "Update code"
   git push
   ```
3. Railway автоматически обнаружит изменения и перезапустит сервис

## 🐛 Если что-то не работает:

1. **Проверьте Logs:**
   - Settings → Logs
   - Ищите ошибки (красным цветом)

2. **Проверьте Variables:**
   - Убедитесь, что все переменные установлены (если используете)

3. **Проверьте Deployments:**
   - Settings → Deployments
   - Убедитесь, что последний деплой успешен (зеленая галочка)
