# Инструкция: Загрузка в Git и деплой на Railway

## 📤 Шаг 1: Добавить изменения в Git

```bash
# Перейти в директорию проекта
cd "D:\Code\Python\rofl tg bot"

# Проверить статус (какие файлы изменены)
git status

# Добавить все изменения
git add .

# Или добавить конкретные файлы:
git add config.py bot.py
```

## 💾 Шаг 2: Создать коммит

```bash
# Создать коммит с описанием изменений
git commit -m "Update config.py with Railway URL and fix EternalMod name"
```

## 🚀 Шаг 3: Загрузить в репозиторий

```bash
# Если это первый раз (еще нет remote):
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main

# Если remote уже настроен:
git push
```

## 🌐 Шаг 4: Деплой на Railway

### Если проект уже подключен к Railway:

1. **Railway автоматически обнаружит изменения:**
   - Откройте Railway → ваш проект
   - Railway автоматически начнет новый деплой
   - Дождитесь завершения (зеленая галочка)

2. **Проверьте логи:**
   - Откройте вкладку **"Logs"**
   - Должна быть строка: `HTTP server started on http://0.0.0.0:XXXX`
   - Должна быть строка: `Bot starting polling...`

3. **Проверьте работу:**
   - Откройте веб-приложение: `https://rofl-bot-production.up.railway.app/`
   - Проверьте бота в Telegram

### Если проект еще не подключен:

1. **Создайте проект на Railway:**
   - Зайдите на https://railway.app
   - Нажмите **"New Project"**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий

2. **Настройте домен:**
   - Откройте ваш сервис → **Settings**
   - Найдите **"Networking"** → **"Generate Domain"**
   - Скопируйте URL

3. **Обновите WEBAPP_URL (если нужно):**
   - Если URL отличается от `rofl-bot-production.up.railway.app`
   - Обновите в `config.py` и сделайте новый коммит:
   ```bash
   git add config.py
   git commit -m "Update WEBAPP_URL"
   git push
   ```

## ✅ Проверка после деплоя

1. **В Railway:**
   - Deployments → последний деплой должен быть успешным (зеленая галочка)
   - Logs → нет ошибок

2. **В Telegram:**
   - Отправьте боту `/start`
   - Нажмите кнопку "📊 Дашборд"
   - Мини-приложение должно открыться

3. **В браузере:**
   - Откройте `https://rofl-bot-production.up.railway.app/`
   - Должна открыться главная страница

## 🔄 Обновление кода в будущем

Когда нужно обновить код:

```bash
# 1. Внесите изменения в файлы
# 2. Добавьте в Git
git add .

# 3. Создайте коммит
git commit -m "Описание изменений"

# 4. Загрузите в репозиторий
git push

# 5. Railway автоматически перезапустит бота
```

## 🐛 Если что-то пошло не так

1. **Проверьте логи в Railway:**
   - Откройте Logs
   - Ищите ошибки (красным)

2. **Проверьте, что файлы загружены:**
   ```bash
   git status
   git log --oneline -5
   ```

3. **Проверьте на GitHub:**
   - Убедитесь, что файлы есть в репозитории
   - Проверьте, что `config.py` содержит правильные значения

## 📝 Быстрая шпаргалка

```bash
# Полный цикл обновления:
git add .
git commit -m "Update code"
git push
# Railway автоматически задеплоит
```
