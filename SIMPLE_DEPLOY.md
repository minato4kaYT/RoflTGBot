# Простое развертывание с config.py

## ✅ Упрощенный способ

Теперь `config.py` загружается в репозиторий для упрощения развертывания.

## ⚠️ ВАЖНО: Безопасность

- ✅ **Если репозиторий ПРИВАТНЫЙ** - можно безопасно загружать config.py
- ❌ **Если репозиторий ПУБЛИЧНЫЙ** - НЕ загружайте config.py с реальными токенами!

## 🚀 Развертывание на Railway

### Шаг 1: Подготовка

1. **Отредактируйте config.py:**
   - Укажите ваш `BOT_TOKEN`
   - Укажите ваш `OWNER_ID`
   - Настройте канал и другие параметры

2. **Для Railway обновите WEBAPP_URL:**
   - После деплоя Railway создаст URL
   - Обновите `WEBAPP_URL` в config.py на этот URL
   - Или используйте переменную окружения `WEBAPP_URL` на Railway

### Шаг 2: Загрузка в Git

```bash
git add config.py
git commit -m "Add config.py"
git push
```

### Шаг 3: Развертывание на Railway

1. **Создайте проект:**
   - Зайдите на https://railway.app
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий

2. **Настройте домен:**
   - Откройте Settings → Generate Domain
   - Скопируйте созданный URL

3. **Обновите WEBAPP_URL (опционально):**
   - Можно обновить в config.py и сделать новый коммит
   - Или добавить переменную окружения `WEBAPP_URL` на Railway

4. **Готово!** Railway автоматически:
   - Установит зависимости
   - Запустит бота
   - HTTP сервер будет доступен по публичному URL

## 🔄 Обновление WEBAPP_URL на Railway

После первого деплоя:

1. Скопируйте URL, который создал Railway
2. Добавьте переменную окружения на Railway:
   - Settings → Variables
   - Добавьте: `WEBAPP_URL=https://your-app.up.railway.app/`
3. Или обновите в config.py и сделайте новый коммит

## 📝 Что работает автоматически

- ✅ Бот запускается с настройками из config.py
- ✅ HTTP сервер запускается на порту из переменной `PORT` (Railway устанавливает автоматически)
- ✅ Веб-приложение доступно по публичному URL Railway
- ✅ Переменные окружения имеют приоритет над значениями в config.py

## 🔐 Если нужно обновить токен

1. Обновите `BOT_TOKEN` в config.py
2. Сделайте коммит и пуш
3. Railway автоматически перезапустит бота

Или используйте переменную окружения `BOT_TOKEN` на Railway (имеет приоритет).
