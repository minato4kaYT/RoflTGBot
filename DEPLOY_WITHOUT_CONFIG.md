# Развертывание без config.py (безопасный способ)

## 🎯 Принцип работы

Ваш `config.py` уже настроен для работы с **переменными окружения**. Это означает:
- ✅ `config.py` можно безопасно загружать в Git (он не содержит токенов)
- ✅ Все секретные данные задаются через переменные окружения на хостинге
- ✅ Для локальной разработки используйте `config.example.py`

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка для Git

1. **Убедитесь, что `config.py` в `.gitignore`** (уже есть ✅)

2. **Создайте `config.py` из примера для локальной разработки:**
   ```bash
   cp config.example.py config.py
   # Затем отредактируйте config.py и вставьте свои токены
   ```

3. **Закоммитьте проект:**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push
   ```

### Шаг 2: Развертывание на хостинге

#### Вариант A: Railway

1. **Создайте проект на Railway:**
   - Зайдите на https://railway.app
   - New Project → Deploy from GitHub repo
   - Выберите ваш репозиторий

2. **Настройте сервис:**
   - Railway автоматически определит, что это веб-сервис (по порту)
   - Откройте настройки сервиса → Settings
   - Включите "Generate Domain" чтобы получить публичный URL

3. **Добавьте переменные окружения:**
   - Откройте настройки проекта → Variables
   - Добавьте следующие переменные:

   ```
   BOT_TOKEN=ваш_токен_бота
   OWNER_ID=ваш_telegram_id
   REQUIRED_CHANNEL=@your_channel
   REQUIRED_CHANNEL_URL=https://t.me/your_channel
   ```

4. **Настройте WEBAPP_URL:**
   - После деплоя Railway создаст публичный URL (например: `https://your-app.up.railway.app`)
   - Скопируйте этот URL
   - Добавьте переменную окружения:
   ```
   WEBAPP_URL=https://your-app.up.railway.app/
   ```
   - ⚠️ Важно: URL должен заканчиваться на `/`

5. **Готово!** Railway автоматически:
   - Клонирует ваш репозиторий
   - Установит зависимости из `requirements.txt`
   - Запустит `bot.py`
   - HTTP сервер запустится на порту из переменной `PORT` (Railway устанавливает её автоматически)
   - Веб-приложение будет доступно по публичному URL

#### Вариант B: Render

1. **Создайте Web Service:**
   - Зайдите на https://render.com
   - New → Web Service
   - Подключите GitHub репозиторий

2. **Настройки:**
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python bot.py`
   - **Environment:** Python 3

3. **Добавьте переменные окружения:**
   - Settings → Environment Variables
   - Добавьте те же переменные, что и для Railway

4. **Готово!**

#### Вариант C: VPS (Ubuntu/Debian)

1. **Подключитесь к серверу:**
   ```bash
   ssh user@your-server-ip
   ```

2. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/your-username/your-repo.git rofl-bot
   cd rofl-bot
   ```

3. **Создайте виртуальное окружение:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   pip install -r requirements.txt
   ```

4. **Создайте файл с переменными окружения:**
   ```bash
   nano .env
   ```
   
   Добавьте:
   ```
   BOT_TOKEN=ваш_токен_бота
   OWNER_ID=ваш_telegram_id
   REQUIRED_CHANNEL=@your_channel
   REQUIRED_CHANNEL_URL=https://t.me/your_channel
   WEBAPP_URL=https://your-domain.com/
   ```

5. **Обновите systemd service:**
   
   Отредактируйте `/etc/systemd/system/rofl-bot.service`:
   ```ini
   [Unit]
   Description=ROFL Telegram Bot
   After=network.target

   [Service]
   Type=simple
   User=botuser
   WorkingDirectory=/home/botuser/rofl-bot
   Environment="PATH=/home/botuser/rofl-bot/venv/bin"
   EnvironmentFile=/home/botuser/rofl-bot/.env
   ExecStart=/home/botuser/rofl-bot/venv/bin/python bot.py
   Restart=always
   RestartSec=10

   [Install]
   WantedBy=multi-user.target
   ```

   Обратите внимание на строку `EnvironmentFile=/home/botuser/rofl-bot/.env` - она загружает переменные из файла.

6. **Запустите сервис:**
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable rofl-bot
   sudo systemctl start rofl-bot
   ```

#### Вариант D: Docker

1. **Создайте `.env` файл:**
   ```bash
   BOT_TOKEN=ваш_токен_бота
   OWNER_ID=ваш_telegram_id
   REQUIRED_CHANNEL=@your_channel
   REQUIRED_CHANNEL_URL=https://t.me/your_channel
   WEBAPP_URL=https://your-domain.com/
   ```

2. **Запустите контейнер:**
   ```bash
   docker build -t rofl-bot .
   docker run -d --env-file .env --name rofl-bot rofl-bot
   ```

## 🔐 Безопасность

### ✅ Что МОЖНО загружать в Git:
- `config.example.py` (без реальных токенов)
- `bot.py`
- `requirements.txt`
- Все остальные файлы проекта

### ❌ Что НЕЛЬЗЯ загружать в Git:
- `config.py` с реальными токенами (уже в `.gitignore`)
- `.env` файлы (уже в `.gitignore`)
- `business_connections.json` (уже в `.gitignore`)

## 🧪 Проверка перед развертыванием

Запустите скрипт проверки:
```bash
python check_config.py
```

Он проверит, что все необходимые переменные настроены.

## 📝 Список необходимых переменных окружения

| Переменная | Описание | Пример |
|-----------|----------|--------|
| `BOT_TOKEN` | Токен бота от @BotFather | `123456:ABC-DEF...` |
| `OWNER_ID` | Ваш Telegram ID | `123456789` |
| `REQUIRED_CHANNEL` | Канал для подписки | `@your_channel` |
| `REQUIRED_CHANNEL_URL` | URL канала | `https://t.me/your_channel` |
| `WEBAPP_URL` | URL мини-приложения | `https://your-app.railway.app/` |
| `PORT` | Порт для веб-сервера (опционально) | `8080` |

## 🚀 Быстрый старт

1. **Локально:**
   ```bash
   cp config.example.py config.py
   # Отредактируйте config.py
   python bot.py
   ```

2. **На хостинге:**
   - Загрузите код в Git
   - Добавьте переменные окружения на хостинге
   - Запустите бота

## ❓ FAQ

**Q: Что если я случайно залил config.py с токенами?**
A: Немедленно:
1. Смените токен бота в @BotFather
2. Удалите config.py из истории Git: `git rm --cached config.py`
3. Убедитесь, что он в `.gitignore`

**Q: Как узнать свой Telegram ID?**
A: Напишите боту @userinfobot или @getidsbot

**Q: Где взять токен бота?**
A: Создайте бота через @BotFather в Telegram

**Q: Можно ли использовать .env файл?**
A: Да! Но убедитесь, что `.env` в `.gitignore` (уже есть ✅)
