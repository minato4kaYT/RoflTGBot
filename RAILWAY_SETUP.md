# Настройка Railway для веб-приложения

## ✅ Да, веб-часть будет работать на Railway!

Ваш бот уже настроен для работы с Railway. HTTP сервер автоматически запускается вместе с ботом.

## 🚀 Быстрая настройка

### Шаг 1: Создание проекта

1. Зайдите на https://railway.app
2. New Project → Deploy from GitHub repo
3. Выберите ваш репозиторий

### Шаг 2: Настройка домена

1. Откройте ваш сервис → Settings
2. Включите "Generate Domain"
3. Railway создаст публичный URL (например: `https://rofl-bot-production.up.railway.app`)
4. Скопируйте этот URL

### Шаг 3: Переменные окружения

Откройте Variables и добавьте:

```
BOT_TOKEN=ваш_токен_бота
OWNER_ID=ваш_telegram_id
REQUIRED_CHANNEL=@your_channel
REQUIRED_CHANNEL_URL=https://t.me/your_channel
WEBAPP_URL=https://rofl-bot-production.up.railway.app/
```

⚠️ **Важно:**
- `WEBAPP_URL` должен заканчиваться на `/`
- `PORT` НЕ нужно указывать - Railway автоматически установит эту переменную

### Шаг 4: Деплой

Railway автоматически:
1. Определит Python проект
2. Установит зависимости из `requirements.txt`
3. Запустит `python bot.py`
4. HTTP сервер запустится на порту из `PORT` (Railway устанавливает автоматически)
5. Веб-приложение будет доступно по публичному URL

## 🔍 Как это работает

1. **При запуске `bot.py`:**
   - Запускается HTTP сервер на порту из переменной `PORT` (Railway устанавливает автоматически)
   - Затем запускается Telegram бот
   - Оба работают параллельно

2. **Railway определяет веб-сервис:**
   - По порту, который слушает приложение
   - Автоматически создает публичный URL
   - Настраивает проксирование трафика

3. **Веб-приложение доступно:**
   - По URL, который Railway создал
   - API: `https://your-app.up.railway.app/api/messages`
   - Статика: `https://your-app.up.railway.app/`

## 📝 Проверка работы

После деплоя:

1. **Проверьте логи:**
   - В Railway откройте Deployments → View Logs
   - Должна быть строка: `HTTP server started on http://0.0.0.0:XXXX`

2. **Проверьте веб-приложение:**
   - Откройте URL в браузере
   - Должна открыться главная страница мини-приложения

3. **Проверьте API:**
   - Можно протестировать через curl или Postman
   - `POST https://your-app.up.railway.app/api/messages`

## 🐛 Troubleshooting

### Веб-приложение не открывается

1. **Проверьте логи:**
   - Убедитесь, что HTTP сервер запустился
   - Проверьте, нет ли ошибок

2. **Проверьте переменную PORT:**
   - Railway автоматически устанавливает `PORT`
   - Не нужно указывать её вручную

3. **Проверьте WEBAPP_URL:**
   - Должен заканчиваться на `/`
   - Должен быть правильный домен Railway

### Бот не отвечает

1. **Проверьте BOT_TOKEN:**
   - Убедитесь, что токен правильный
   - Проверьте в логах, что бот запустился

2. **Проверьте логи:**
   - Railway → Deployments → View Logs
   - Ищите ошибки запуска

## 💡 Полезные советы

1. **Используйте Railway CLI для локальной разработки:**
   ```bash
   railway login
   railway link
   railway up
   ```

2. **Мониторинг:**
   - Railway показывает метрики использования
   - Можно настроить алерты

3. **Резервное копирование:**
   - Регулярно сохраняйте `business_connections.json`
   - Используйте Railway Volumes для постоянного хранения

## 📚 Дополнительно

- [Railway Docs](https://docs.railway.app)
- [Railway Discord](https://discord.gg/railway)
